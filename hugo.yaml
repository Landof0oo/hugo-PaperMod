baseURL: "https://landofooo.vercel.app/"
languageCode: "zh-cn"
title: "löö's land"

# 导航栏配置
menu:
  main:
    - name: "All"
      url: "posts"
      weight: 10

params:
  label:
    text: "löö's land"
  
  # 1. 图标设置（确保 static 里的文件名叫 favicon.ico）
  assets:
    favicon: "/favicon.ico"
  
  # 2. 导航栏功能
  ShowSearchButton: true
  disableThemeToggle: false
  ShowRSSButtonText: false

  # 3. 极简列表模式
  showFullContents: false
  summaryLength: 0
